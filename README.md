# Boilerplate details

branchs :
 - master
 - dev

master contains:
  - dist folder with builded website

dev contains:
  - src folder with all sources fils without compression

## Usage

```javascript
// DEV:
npx gulp watch

// DIST:
npx gulp build
```
